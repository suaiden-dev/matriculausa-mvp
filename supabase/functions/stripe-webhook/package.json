{
  "name": "stripe-webhook",
  "version": "1.0.0",
  "description": "Supabase Edge Function to handle Stripe webhooks",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "install:deps": "npm install"
  },
  "dependencies": {
    "stripe": "^14.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
} 